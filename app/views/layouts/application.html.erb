<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Malaysian RE Analysis</title>
  <%= csrf_meta_tags %>
  <%= csp_meta_tag %>

  <!-- Bootstrap CSS -->
  <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
  <%= stylesheet_link_tag 'property_roi_calculator', media: 'all', 'data-turbolinks-track': 'reload' %>

  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css">

  <!-- Custom Orange Theme Styles -->
  <style>
    body {
      background-color: #fff8f1;
      color: #212529;
      font-family: 'Roboto', sans-serif;
    }

    .navbar-dark-bg {
      background-color: #212529 !important; /* dark gray/navy */
    }

    .navbar-dark-bg .navbar-brand,
    .navbar-dark-bg .nav-link {
      color: #fd7e14 !important; /* bright orange */
    }

    .navbar-dark-bg .nav-link.active {
      background-color: rgba(253, 126, 20, 0.1);
      border-radius: 0.375rem;
      font-weight: 600;
    }

    .container-fluid {
      padding: 2rem;
    }

    .content-wrapper {
      background: white;
      border-radius: 0.5rem;
      box-shadow: 0 0.125rem 0.25rem rgba(0, 0, 0, 0.075);
      padding: 2rem;
      margin-bottom: 2rem;
    }

    footer {
      background-color: #cc5803;
      color: white;
      padding: 1.5rem 0;
      margin-top: 2rem;
    }

    .alert-success {
      background-color: #dff3e4;
      color: #198754;
    }

    .alert-danger {
      background-color: #f8d7da;
      color: #842029;
    }

    /* Orange color utility classes */
    .bg-orange-50 { background-color: #fff8f1; }
    .bg-orange-100 { background-color: #ffe8cc; }
    .bg-orange-200 { background-color: #ffd8a8; }
    .bg-orange-600 { background-color: #fd7e14; }
    .text-orange-600 { color: #fd7e14; }
    .text-orange-700 { color: #e76b08; }
    .text-orange-800 { color: #cc5803; }
    .btn-orange-600 {
      background-color: #fd7e14;
      border-color: #fd7e14;
      color: white;
    }
    .btn-orange-600:hover {
      background-color: #e76b08;
      border-color: #e76b08;
    }
    .footer-dark-orange {
      background-color: #212529; /* dark footer */
      color: #adb5bd; /* light gray text */
    }

    .footer-dark-orange .text-orange {
      color: #fd7e14 !important; /* strong orange */
    }

    .footer-dark-orange a {
      color: #fd7e14;
      text-decoration: none;
    }

    .footer-dark-orange a:hover {
      text-decoration: underline;
    }


    @media (max-width: 991.98px) {
      .navbar-links {
        flex-direction: column;
        width: 100%;
        margin-top: 1rem;
      }

      .nav-link {
        margin: 0.25rem 0;
      }

      .navbar-menu {
        width: 100%;
      }
    }
  </style>

  <!-- Chart.js -->
  <script src="https://cdn.jsdelivr.net/npm/chart.js@3.9.1/dist/chart.min.js"></script>
  <%= javascript_include_tag "application", "data-turbo-track": "reload" %>
</head>

<body>
  <header>
    <!-- Main Navigation with Orange Theme -->
    <nav class="navbar navbar-expand-lg navbar-dark-bg">
      <div class="container-fluid">
        <%= link_to root_path, class: "navbar-brand" do %>
          <i class="fas fa-home me-1"></i> Malaysian RE Analysis
        <% end %>

        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown">
          <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="navbarNavDropdown">
          <ul class="navbar-nav ms-auto">
            <li class="nav-item">
              <%= link_to property_roi_path, class: "nav-link #{current_page?(property_roi_path) ? 'active' : ''}" do %>
                <i class="fas fa-chart-line me-1"></i> ROI Calculator
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to affordability_path, class: "nav-link #{current_page?(affordability_path) ? 'active' : ''}" do %>
                <i class="fas fa-calculator me-1"></i> Affordability Calculator
              <% end %>
            </li>
            <li class="nav-item">
              <%= link_to mortgage_path, class: "nav-link #{current_page?(mortgage_path) ? 'active' : ''}" do %>
                <i class="fas fa-money-bill-wave me-1"></i> Mortgage Calculator
              <% end %>
            </li>
          </ul>
        </div>
      </div>
    </nav>
  </header>

  <main class="container-fluid">
    <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert <%= type == 'notice' ? 'alert-success' : 'alert-danger' %> alert-dismissible fade show" role="alert">
          <%= message %>
          <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
        </div>
      <% end %>

      <div class="content-wrapper">
        <%= yield %>
      </div>
    </div>
  </main>

  <footer class="footer-dark-orange mt-auto py-3">
    <div class="container text-center">
      <p class="mb-1 text-orange fw-semibold">© <%= Date.today.year %> Malaysian RE Analysis</p>
    </div>
  </footer>


  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
